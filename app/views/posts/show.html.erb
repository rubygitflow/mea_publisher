<% if notice.present? %>
  <p class="mb mt message message-notice text-green"><%= notice %></p>
<% end %>

<div class="container">
  <div class="text-title mb title-lg">
    <div>
      Post
    </div>

    <div>
      <%= link_to fa_icon('bars'), posts_path, title: "Back to posts" %>
    </div>
  </div>

  <%= render @post %>
</div>
